---
machine:
  python:
    version: 3.6.1
test:
  pre:
    - pip install -r test-requirements.txt
  override:
    - ./lintme

deployment:
  s3:
    branch: /(develop)|(staging)|(master)/
    commands:
      - bash scripts/upload_blueprints.sh
